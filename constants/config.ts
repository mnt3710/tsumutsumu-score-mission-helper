/**
 * アプリケーション全体で使用する定数・設定値
 * ハードコードを避け、一元管理を実現
 */

// =============================================================================
// アプリケーション情報
// =============================================================================
export const APP_CONFIG = {
  name: 'ツムツム スコアミッション 計算ツール',
  subtitle: 'スコアの下一桁を○にしよう',
  version: 'v0.1.0',
  description: '特定の一の位にするための計算補助ツール',
} as const;

// =============================================================================
// 入力値の範囲設定
// =============================================================================
export const INPUT_LIMITS = {
  level: {
    min: 1,
    max: 900,
    default: 3,
  },
  targetDigit: {
    min: 0,
    max: 9,
    default: 0,
  },
  tsumScore: {
    min: 1,
    max: 10000,
    default: 2000,
    searchMin: 50,
  },
  baseScore: {
    min: 0,
    max: 100000,
    searchLimit: 5000,
  },
} as const;

// =============================================================================
// チェーン設定
// =============================================================================
export const CHAIN_CONFIG = {
  /** 攻略提案で使用するチェーン数の配列 */
  suggestChains: [3, 4, 5] as const,
  /** チェーン数ごとのスコア */
  scores: {
    3: 300,
    4: 700,
    5: 1300,
    6: 2100,
    7: 3100,
    8: 4600,
  } as const,
} as const;

// =============================================================================
// 表示設定
// =============================================================================
export const DISPLAY_CONFIG = {
  /** 攻略提案で表示する最大候補数 */
  maxStrategyCandidates: 30,
  /** 目標スコア一覧で表示する最大件数 */
  maxTargetListItems: 200,
  /** ステップ選択のオプション */
  stepOptions: [
    { value: 1, label: '単位表示 (1)' },
    { value: 10, label: '10刻み' },
    { value: 100, label: '100刻み' },
  ],
} as const;

// =============================================================================
// タブ設定
// =============================================================================
export const TAB_CONFIG = {
  items: [
    { id: 'current-check', label: '現在スコア判定', icon: '📊' },
    { id: 'strategy-suggest', label: '攻略方法提案', icon: '🎯' },
    { id: 'target-list', label: '目標達成スコア一覧', icon: '✨' },
  ],
} as const;

// =============================================================================
// UIテーマ設定
// =============================================================================
export const THEME = {
  colors: {
    primary: {
      light: 'rgb(238 242 255)', // indigo-50
      main: 'rgb(99 102 241)',   // indigo-500
      dark: 'rgb(79 70 229)',    // indigo-600
    },
    accent: {
      light: 'rgb(253 242 248)', // pink-50
      main: 'rgb(236 72 153)',   // pink-500
      dark: 'rgb(219 39 119)',   // pink-600
    },
    success: {
      light: 'rgb(240 253 244)', // green-50
      main: 'rgb(34 197 94)',    // green-500
      dark: 'rgb(22 163 74)',    // green-600
    },
    error: {
      light: 'rgb(254 242 242)', // red-50
      main: 'rgb(239 68 68)',    // red-500
      dark: 'rgb(220 38 38)',    // red-600
    },
  },
  animation: {
    fast: '120ms',
    base: '200ms',
    slow: '300ms',
  },
} as const;

// =============================================================================
// ツムスコアの一般的な範囲（ヒント表示用）
// =============================================================================
export const TSUM_SCORE_INFO = {
  /** 一般的なツムスコアの最小値 */
  typicalMin: 60,
  /** 一般的なツムスコアの最大値 */
  typicalMax: 2000,
  /** ヒントメッセージ */
  hint: '探索最大値を小さくすると、現実的なツムの候補が上位に表示されます。一般的なツムスコアの範囲は 10〜2000 程度です。',
} as const;
